import QueueView from "@/components/views/QueueView";

export const metadata = { title: "Discrepancy & Risk - Mirror Trade Analytics" };

export default function RiskPage() {
  return <QueueView />;
}
