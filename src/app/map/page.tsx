import MapView from "@/components/views/MapView";

export const metadata = {
  title: "Risk Map — Trade Mirror",
  description:
    "Anomaly × evidence matrix and geographic view of residual unexplained trade discrepancies for Uzbekistan — statistical screening signals, not findings of wrongdoing.",
};

export default function MapPage() {
  return <MapView />;
}
